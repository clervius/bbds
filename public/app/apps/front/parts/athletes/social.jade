.row
	.col-md-12
		h4.title Contact Options
		ul.list-lined
			li(ng-repeat="account in scMedia")
				span(class="{{account.service}}")
					i.fa(class="fa-{{account.service}}")
				a.serviceLink(href="{{account.link}}") {{ account.link }}